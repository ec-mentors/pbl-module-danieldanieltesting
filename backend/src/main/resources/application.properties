spring.application.name=promptdex-backend


# Server Port
server.port=8080

# PostgreSQL Database Configuration
# Replace the placeholders with your REAL credentials
spring.datasource.url=jdbc:postgresql://localhost:5432/promptdex_db
spring.datasource.username=promptdex_user
spring.datasource.password=Mr$lp0-kPln2%0pWE09vD5ka

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Add this line to be explicit. It helps prevent the "Unable to determine Dialect" issue.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Secret Key
jwt.secret=YourSuperSecretKeyForJWTsThatIsVeryLongAndSecure

# --- JACKSON CONFIGURATION ---
# This forces Jackson to write all date/time objects as standard ISO-8601 strings
# instead of numeric timestamps. This is the standard, most compatible format.
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false